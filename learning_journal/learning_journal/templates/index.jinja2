{% extends 'layout.jinja2' %}
{% block content %}
    {% for entry in entries %}
        <h1>{{ entry.title }}</h1>
        <p>{{ entry.body }}</p>
        <button><a href="{{ request.route_url('detail_view', id=entry.id) }}">View Entry</a></button>
    {% endfor %}
{% endblock %}
